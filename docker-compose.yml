networks:
  prod:

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
    volumes:
      - './nginx.conf:/etc/nginx.conf'
    depends_on:
      - frontend
    networks:
      - prod

  frontend:
    build:
      context: ./front
    networks:
      - prod
